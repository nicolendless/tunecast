[tool.poetry]
name = "tunecast"
version = "0.1.0"
description = ""
authors = ["Nicole Vicente <nicolevs94@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
fastapi = {version = ">=0.113.0,<0.114.0", extras = ["standard"]}
pydantic = ">=2.7.0,<3.0.0"
dependency-injector = ">=4.0,<5.0"
requests = "^2.32.3"
pydantic-settings = "^2.6.1"

[tool.poetry.group.dev.dependencies]
ipdb = "^0.13.13"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
